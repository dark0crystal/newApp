import { NavLink } from "react-router-dom"

export default function Brand(){
    return(
        <div>
            <NavLink to='/' className="mx-3 text-dark">
                Volunteer
            </NavLink>
        </div>
    )
}